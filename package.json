{
  "name": "minecraft-bedrock-server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "all": "yarn run in && yarn run clean && yarn run build && yarn run start",
    "in": "yarn install --silent",
    "start": "node ./dist",
    "clean": "rm -rf ./dist",
    "fclean": "yarn run clean && rm -rf ./node_modules ./minecraft* ./version.txt",
    "build": "npx tsc"
  },
  "dependencies": {
    "@types/node": "^20.3.1",
    "axios": "^1.4.0",
    "tslog": "^4.8.2",
    "typescript": "^5.1.3"
  }
}
